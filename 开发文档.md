# 贪吃蛇游戏 - 开发文档

## 开发环境搭建

### 1.1 环境准备

#### 必需软件
- **JDK**：Java Development Kit 8 或更高版本
- **IDE**：IntelliJ IDEA / Eclipse / VS Code
- **构建工具**：Maven / Gradle（可选）

#### 项目创建
```bash
# 创建项目目录
mkdir SnakeGame
cd SnakeGame

# 创建标准 Java 项目结构
mkdir -p src/com/SnakeGame/game
mkdir -p src/images
```

#### IDE 配置
1. **IntelliJ IDEA**：
   - File → New → Project
   - 选择 Java 项目
   - 设置项目名称：SnakeGame
   - 设置 JDK 版本：1.8+

2. **Eclipse**：
   - File → New → Java Project
   - 设置项目名称：SnakeGame
   - 选择 JRE：JavaSE-1.8+

---

## 开发流程

### 2.1 项目结构设计

```
SnakeGame/
├── src/
│   └── com/SnakeGame/game/
│       ├── Main.java          # 程序入口
│       ├── GamePanel.java      # 游戏主面板
│       ├── MenuPanel.java      # 菜单面板
│       ├── RankPanel.java      # 排行榜面板
│       ├── Snake.java         # 蛇类
│       ├── Food.java          # 食物类
│       └── Images.java        # 图片资源
├── images/               # 游戏图片资源
└── rankings.dat          # 排行榜数据文件（自动生成）
```

### 2.2 核心类开发顺序

#### 第一步：Main.java（程序入口）
**开发内容**：
1. 创建 CardLayout 管理页面切换
2. 创建主面板容器
3. 实现窗口创建和配置
4. 实现 turnPage() 页面切换方法
5. 实现游戏面板焦点管理

**关键代码**：
```java
public class Main {
    private static CardLayout cardLayout = new CardLayout();
    private static JPanel mainPanel = new JPanel(cardLayout);
    
    public static void main(String[] args) {
        // 创建面板
        GamePanel gamePanel = new GamePanel();
        MenuPanel menuPanel = new MenuPanel();
        RankPanel rankPanel = new RankPanel();
        
        // 添加到主容器
        mainPanel.add(gamePanel, "game");
        mainPanel.add(menuPanel, "menu");
        mainPanel.add(rankPanel, "rank");
        
        // 创建窗口
        JFrame frame = new JFrame("贪吃蛇游戏");
        frame.add(mainPanel);
        frame.setVisible(true);
    }
    
    public static void turnPage(String name) {
        cardLayout.show(mainPanel, name);
    }
}
```

#### 第二步：Snake.java（蛇类）
**开发内容**：
1. 定义蛇的坐标数组（skx[], sky[]）
2. 定义蛇的长度和方向
3. 实现 init() 初始化方法
4. 实现 move() 移动方法
5. 实现 touchJudge() 碰撞检测方法
6. 实现 keyPressed() 键盘控制方法

**关键代码**：
```java
public class Snake {
    public static int[] skx = new int[200];
    public static int[] sky = new int[200];
    private static int length = 10;
    private static String direction = "R";
    
    public static void init() {
        length = 10;
        int k = 300;
        for (int i = 0; i < length; i++, k -= 25) {
            skx[i] = k;
            sky[i] = 300;
        }
        direction = "R";
    }
    
    public static void move() {
        for (int i = length - 1; i > 0; i--) {
            skx[i] = skx[i - 1];
            sky[i] = sky[i - 1];
        }
        
        switch (direction) {
            case "R": skx[0] += 25; break;
            case "L": skx[0] -= 25; break;
            case "U": sky[0] -= 25; break;
            case "D": sky[0] += 25; break;
        }
    }
}
```

#### 第三步：Food.java（食物类）
**开发内容**：
1. 定义食物坐标（fx, fy）
2. 定义速度变化率（speedChangeRate）
3. 实现 init() 初始化方法
4. 实现 create() 随机生成方法
5. 实现 eat() 吃食物方法
6. 实现 Judge() 碰撞检测方法

**关键代码**：
```java
public class Food {
    private static int fx = 600;
    private static int fy = 300;
    private static int speedChangeRate = 5;
    
    public static void create() {
        Random random = new Random();
        fx = random.nextInt(31) * 25;
        fy = random.nextInt(22) * 25 + 50;
        if (!Judge(fx, fy)) {
            create();
        }
    }
    
    public static void eat() {
        if (fx == Snake.skx[0] && fy == Snake.sky[0]) {
            Snake.setLength(Snake.getLength() + 1);
            GamePanel.setScore(GamePanel.getScore() + 10);
            GamePanel.setSpeed(GamePanel.getSpeed() - speedChangeRate);
            create();
        }
    }
}
```

#### 第四步：GamePanel.java（游戏主面板）
**开发内容**：
1. 定义游戏状态变量（isStart, isDead, score, speed）
2. 创建定时器（Timer）
3. 创建游戏按钮
4. 实现 paintComponent() 绘制方法
5. 实现游戏逻辑方法
6. 实现按钮管理方法

**关键代码**：
```java
public class GamePanel extends JPanel {
    private static boolean isStart = false;
    private static boolean isDead = false;
    private static int score = 0;
    private static int speed = 250;
    public static Timer timer;
    
    public GamePanel() {
        initializeGameState();
        setupPanelProperties();
        setupButtons();
        addKeyboardListener();
        initializeTimer();
        timer.start();
    }
    
    @Override
    protected void paintComponent(Graphics g) {
        super.paintComponent(g);
        drawBackground(g);
        drawGameArea(g);
        drawFood(g);
        drawSnake(g);
        drawGameInfo(g);
        drawGameStateMessage(g);
    }
}
```

#### 第五步：MenuPanel.java（菜单面板）
**开发内容**：
1. 定义游戏模式状态变量
2. 创建模式选择按钮
3. 创建排行榜按钮
4. 实现按钮事件监听
5. 实现菜单状态管理方法

**关键代码**：
```java
public class MenuPanel extends JPanel {
    private static String state;
    private static boolean rankingMode = false;
    JButton normalButton = new JButton("Normal Model");
    JButton hardButton = new JButton("Hard Model");
    JButton crazyButton = new JButton("Crazy Model");
    JButton rankingButton = new JButton("Ranking List");
    
    private void setAddMouseListener() {
        normalButton.addMouseListener(new MouseAdapter() {
            public void mouseClicked(MouseEvent e) {
                if (rankingMode) {
                    state = "normal";
                    RankPanel.showRank(state);
                    Main.turnPage("rank");
                } else {
                    state = "normal";
                    Main.turnPage("game");
                }
            }
        });
    }
}
```

#### 第六步：RankPanel.java（排行榜面板）
**开发内容**：
1. 定义三种模式的排行榜数组
2. 创建返回菜单按钮
3. 实现 showRank() 显示方法
4. 实现 updateRank() 更新方法
5. 实现 loadRankings() 加载方法
6. 实现 saveRankings() 保存方法

**关键代码**：
```java
public class RankPanel extends JPanel {
    private static int[] normalRank = {100,90,80,70,60,50,40,20,10,0};
    private static int[] hardRank = {100,90,80,70,60,50,40,20,10,0};
    private static int[] crazyRank = {100,90,80,70,60,50,40,20,10,0};
    private static String currentModel = "normal";
    
    public static void updateRank(int score, String model) {
        if (model.equals("normal")) {
            insertScore(normalRank, score);
        } else if (model.equals("hard")) {
            insertScore(hardRank, score);
        } else if (model.equals("crazy")) {
            insertScore(crazyRank, score);
        }
        saveRankings();
    }
    
    private static void loadRankings() {
        Properties props = new Properties();
        FileInputStream fis = new FileInputStream(RANK_FILE);
        props.load(fis);
        fis.close();
        
        for (int i = 0; i < 10; i++) {
            String key = "normal" + i;
            if (props.containsKey(key)) {
                normalRank[i] = Integer.parseInt(props.getProperty(key, "0"));
            }
        }
    }
}
```

---

## 开发规范

### 3.1 代码规范

#### 命名规范
- **类名**：大驼峰命名（PascalCase），如 GamePanel
- **方法名**：小驼峰命名（camelCase），如 setupButtons()
- **变量名**：小驼峰命名（camelCase），如 menuButton
- **常量名**：全大写下划线（UPPER_SNAKE_CASE），如 SCORE_FONT

#### 注释规范
- **类注释**：使用 Javadoc 格式，说明类的职责
- **方法注释**：说明方法的作用、参数、返回值
- **行内注释**：解释复杂逻辑
- **注释语言**：中文，便于理解

#### 代码格式
- **缩进**：使用 4 个空格缩进
- **行宽**：每行不超过 120 个字符
- **空行**：方法之间保留一个空行
- **大括号**：左大括号不换行，右大括号另起一行

### 3.2 异常处理

#### 异常捕获
```java
try {
    // 可能抛出异常的代码
} catch (FileNotFoundException e) {
    // 文件不存在异常
    System.out.println("文件不存在，使用默认数据");
} catch (IOException e) {
    // IO 异常
    System.err.println("IO 错误: " + e.getMessage());
} catch (NumberFormatException e) {
    // 数字格式异常
    System.err.println("数字格式错误: " + e.getMessage());
}
```

#### 异常处理原则
1. **具体捕获**：捕获具体的异常类型
2. **合理处理**：提供默认值或错误提示
3. **日志记录**：使用 System.out/err 记录
4. **不影响运行**：异常不应导致程序崩溃

### 3.3 资源管理

#### 图片资源加载
```java
public class Images {
    public static ImageIcon title;
    public static ImageIcon food;
    public static ImageIcon body;
    public static ImageIcon up;
    public static ImageIcon down;
    public static ImageIcon left;
    public static ImageIcon right;
    
    static {
        try {
            title = new ImageIcon("images/title.jpg");
            food = new ImageIcon("images/food.png");
            body = new ImageIcon("images/body.png");
            up = new ImageIcon("images/up.png");
            down = new ImageIcon("images/down.png");
            left = new ImageIcon("images/left.png");
            right = new ImageIcon("images/right.png");
        } catch (Exception e) {
            System.err.println("图片加载失败: " + e.getMessage());
        }
    }
}
```

#### 资源管理原则
1. **静态加载**：使用静态代码块加载资源
2. **异常处理**：捕获加载失败异常
3. **空值检查**：使用前检查资源是否为 null
4. **路径规范**：使用相对路径，便于部署

---

## 开发技巧

### 4.1 调试技巧

#### 日志输出
```java
// 开发阶段添加调试日志
System.out.println("游戏开始");
System.out.println("蛇移动到: " + skx[0] + ", " + sky[0]);
System.out.println("当前分数: " + score);
```

#### 断点调试
1. **设置断点**：在关键方法入口设置断点
2. **变量观察**：观察变量值的变化
3. **步执行**：逐步执行代码，查看执行流程
4. **条件断点**：设置条件断点，只在特定条件下暂停

#### 常见问题排查
1. **页面不切换**：检查 CardLayout 是否正确配置
2. **按钮无响应**：检查事件监听是否正确添加
3. **键盘无响应**：检查面板是否请求到焦点
4. **数据不保存**：检查文件路径和权限

### 4.2 性能优化

#### 绘制优化
```java
// 使用双缓冲减少闪烁
@Override
protected void paintComponent(Graphics g) {
    super.paintComponent(g);
    // 绘制逻辑
}

// 避免不必要的重绘
// 只在状态变化时调用 repaint()
```

#### 内存优化
```java
// 及时释放资源
BufferedReader reader = new BufferedReader(new FileReader(file));
try {
    // 读取操作
} finally {
    if (reader != null) {
        reader.close();
    }
}

// 使用对象池（可选）
// 重用对象，减少 GC 压力
```

---

## 版本控制

### 5.1 Git 使用

#### 基本操作
```bash
# 初始化仓库
git init

# 添加所有文件
git add .

# 提交代码
git commit -m "实现基础游戏功能"

# 查看状态
git status

# 查看日志
git log
```

#### 分支管理
```bash
# 创建开发分支
git checkout -b feature/game-modes

# 合并分支
git checkout main
git merge feature/game-modes

# 删除分支
git branch -d feature/game-modes
```

---

## 部署说明

### 6.1 打包

#### 使用 IDE 打包
1. **IntelliJ IDEA**：
   - Build → Build Project
   - 生成 JAR 文件在 out/artifacts 目录

2. **Eclipse**：
   - 右键项目 → Export
   - 选择 JAR file
   - 设置导出路径

#### 使用命令行打包
```bash
# 编译 Java 文件
javac -d bin src/com/SnakeGame/game/*.java

# 创建 JAR 文件
jar cvfe SnakeGame.jar Main -C bin .
```

### 6.2 运行

#### 直接运行 JAR
```bash
java -jar SnakeGame.jar
```

#### 使用 Java 命令运行
```bash
# 编译并运行
javac src/com/SnakeGame/game/*.java
java -cp src com.SnakeGame.game.Main
```

---

## 常见问题解决

### 7.1 编译问题

#### 问题：找不到符号
**原因**：包名或类名错误
**解决**：
1. 检查 package 声明是否正确
2. 检查文件是否在正确的包目录下
3. 检查类名是否与文件名一致

#### 问题：图片加载失败
**原因**：图片路径错误或文件不存在
**解决**：
1. 检查 images 目录是否在项目根目录
2. 检查图片文件名是否正确
3. 检查图片格式是否支持

### 7.2 运行问题

#### 问题：窗口不显示
**原因**：未设置窗口可见或添加到面板
**解决**：
1. 检查是否调用 frame.setVisible(true)
2. 检查是否将面板添加到 frame
3. 检查窗口大小是否合理

#### 问题：键盘无响应
**原因**：面板未请求焦点或未添加监听器
**解决**：
1. 检查是否调用 setFocusable(true)
2. 检查是否添加了 KeyListener
3. 检查是否调用了 requestFocusInWindow()

---

## 开发检查清单

### 8.1 功能检查
- [ ] Main 类创建并配置正确
- [ ] Snake 类实现移动和碰撞检测
- [ ] Food 类实现食物生成和碰撞
- [ ] GamePanel 类实现游戏逻辑和绘制
- [ ] MenuPanel 类实现菜单功能
- [ ] RankPanel 类实现排行榜功能
- [ ] 三种游戏模式实现完整
- [ ] 数据持久化功能正常

### 8.2 质量检查
- [ ] 代码符合命名规范
- [ ] 注释完整准确
- [ ] 异常处理完善
- [ ] 资源管理合理
- [ ] 性能优化到位

### 8.3 测试检查
- [ ] 所有功能测试通过
- [ ] 边界情况处理正确
- [ ] 异常情况处理正确
- [ ] 性能指标达标

---

**文档版本**：1.0
**创建日期**：2025-03-04
**维护人**：开发团队
**最后更新**：2025-03-04
