# 贪吃蛇游戏 - 代码功能结构文档

## 项目概述

这是一个基于 Java Swing 的贪吃蛇游戏，包含三种游戏模式（Normal、Hard、Crazy）和排行榜功能。

## 项目结构

```
SnakeGame/
├── src/com/SnakeGame/game/
│   ├── Main.java          # 程序入口，页面管理
│   ├── GamePanel.java      # 游戏主面板，游戏逻辑和渲染
│   ├── MenuPanel.java      # 菜单面板，模式选择
│   ├── RankPanel.java      # 排行榜面板，数据持久化
│   ├── Snake.java         # 蛇类，移动和碰撞检测
│   ├── Food.java          # 食物类，位置生成和碰撞
│   └── Images.java        # 图片资源管理
└── images/               # 游戏图片资源
```

## 核心类详解

### 1. Main.java - 程序入口类

**职责**：程序启动、窗口创建、页面切换管理

**主要成员变量**：
- `cardLayout`：卡片布局管理器，用于切换不同面板
- `mainPanel`：主面板容器，包含所有子面板
- `state`：当前页面状态

**主要方法**：

#### `main(String[] args)`
- **作用**：程序入口方法
- **功能**：
  1. 创建三个面板（GamePanel、MenuPanel、RankPanel）
  2. 创建并配置 JFrame 窗口
  3. 将面板添加到主容器
  4. 显示窗口
  5. 调用 `turnPage()` 显示默认页面

#### `turnPage(String name)`
- **作用**：切换到指定页面
- **参数**：页面名称（"game"、"menu"、"rank"）
- **功能**：
  1. 使用 CardLayout 切换面板
  2. 如果切换到游戏页面，重置游戏状态
  3. 为游戏面板请求焦点，确保键盘事件可用

---

### 2. GamePanel.java - 游戏主面板类

**职责**：游戏逻辑控制、渲染、状态管理、按钮管理

**主要成员变量**：
- `isStart`：游戏是否开始
- `isDead`：蛇是否死亡
- `score`：当前得分
- `speed`：游戏速度（毫秒）
- `scoreUpdate`：分数是否已更新（避免重复记录）
- `timer`：游戏定时器
- `menuButton`：返回菜单按钮
- `rankButton`：查看排名按钮

**主要方法**：

#### `GamePanel()` - 构造函数
- **作用**：初始化游戏面板
- **功能**：
  1. 初始化游戏状态
  2. 设置面板属性
  3. 初始化按钮
  4. 添加键盘监听器
  5. 初始化并启动定时器

#### `initializeGameState()`
- **作用**：初始化游戏状态
- **功能**：
  1. 初始化蛇和食物
  2. 重置游戏状态（未开始、未死亡）
  3. 重置分数和速度
  4. 重置分数更新标志
  5. 隐藏所有按钮

#### `setupPanelProperties()`
- **作用**：设置面板属性
- **功能**：
  1. 设置 null 布局
  2. 设置可聚焦
  3. 设置面板大小

#### `setupButtons()`
- **作用**：初始化游戏按钮
- **功能**：
  1. 设置按钮位置和大小
  2. 设置按钮样式
  3. 添加按钮事件监听
  4. 将按钮添加到面板
- **按钮功能**：
  - 返回菜单：跳转到菜单页面并重置菜单状态
  - 查看排名：跳转到排行榜页面

#### `showButtons()` / `hideButtons()`
- **作用**：控制按钮显示/隐藏
- **使用场景**：
  - 游戏未开始：显示返回菜单按钮
  - 游戏进行中：隐藏所有按钮
  - 游戏结束：显示返回菜单和查看排名按钮

#### `addKeyboardListener()`
- **作用**：添加键盘事件监听
- **功能**：
  1. 监听方向键（WASD、方向键）控制蛇的方向
  2. 监听空格键控制游戏开始/暂停/重新开始
  3. 触发重绘

#### `handleSpaceKey()`
- **作用**：处理空格键事件
- **功能**：
  1. 切换游戏开始/暂停状态
  2. 如果游戏结束，重新开始游戏

#### `resetGame()`
- **作用**：重置游戏状态
- **功能**：
  1. 重置死亡状态
  2. 重置分数和速度
  3. 重新初始化蛇和食物
  4. 重置分数更新标志
  5. 隐藏所有按钮
  6. 重置定时器延迟

#### `initializeTimer()`
- **作用**：初始化游戏定时器
- **功能**：
  1. 创建 Timer 对象，控制游戏循环
  2. 定时器触发时执行游戏逻辑（蛇移动、食物碰撞）
  3. 触发重绘

#### `paintComponent(Graphics g)`
- **作用**：绘制游戏面板
- **功能**：
  1. 绘制背景
  2. 绘制标题
  3. 绘制游戏区域
  4. 绘制食物
  5. 绘制网格
  6. 绘制蛇
  7. 绘制游戏信息
  8. 绘制游戏状态消息

#### `drawGameStateMessage(Graphics g)`
- **作用**：绘制游戏状态消息
- **功能**：
  1. 游戏未开始：显示开始提示，显示返回菜单按钮
  2. 游戏进行中：隐藏所有按钮
  3. 游戏结束：显示结束消息，显示两个按钮

#### `drawStartMessage(Graphics g)` / `drawGameOverMessage(Graphics g)`
- **作用**：绘制开始/结束消息
- **功能**：
  1. 显示提示文字
  2. 显示最终得分
  3. 更新排行榜数据
  4. 显示按钮

---

### 3. MenuPanel.java - 菜单面板类

**职责**：模式选择、菜单状态管理

**主要成员变量**：
- `state`：当前选择的游戏模式
- `isStart`：是否开始游戏
- `rankingMode`：是否处于排行榜选择模式
- `normalButton/hardButton/crazyButton`：模式选择按钮
- `rankingButton`：排行榜按钮

**主要方法**：

#### `MenuPanel()` - 构造函数
- **作用**：初始化菜单面板
- **功能**：
  1. 设置面板属性
  2. 添加所有按钮
  3. 设置按钮事件监听

#### `setupProperties()`
- **作用**：设置面板属性
- **功能**：
  1. 设置 null 布局
  2. 设置背景颜色
  3. 设置可聚焦

#### `addButtons()`
- **作用**：添加按钮到面板
- **功能**：
  1. 设置按钮位置和大小
  2. 设置按钮颜色
  3. 添加按钮到面板

#### `setAddMouseListener()`
- **作用**：设置按钮点击事件
- **功能**：
  1. **Normal 按钮**：
     - 普通模式：开始 Normal 模式游戏
     - 排行榜模式：显示 Normal 排行榜
  2. **Hard 按钮**：
     - 普通模式：开始 Hard 模式游戏（速度更快）
     - 排行榜模式：显示 Hard 排行榜
  3. **Crazy 按钮**：
     - 普通模式：开始 Crazy 模式游戏（速度变化）
     - 排行榜模式：显示 Crazy 排行榜
  4. **Ranking 按钮**：
     - 隐藏自身
     - 进入排行榜选择模式

#### `setButtonProperties(JButton button)`
- **作用**：设置按钮样式
- **功能**：统一设置按钮背景颜色

#### `getState()`
- **作用**：获取当前游戏模式
- **返回**：模式名称（"normal"、"hard"、"crazy"）

#### `resetRankingMode()`
- **作用**：重置排行榜模式
- **功能**：将 `rankingMode` 设为 false

#### `resetMenu()`
- **作用**：重置菜单状态
- **功能**：
  1. 重置排行榜模式
  2. 显示排行榜按钮

---

### 4. RankPanel.java - 排行榜面板类

**职责**：排行榜显示、数据持久化

**主要成员变量**：
- `normalRank/hardRank/crazyRank`：三种模式的排行榜数组（各10个）
- `rank`：当前显示的排行榜
- `currentModel`：当前显示的模式
- `menuButton`：返回菜单按钮
- `RANK_FILE`：排行榜数据文件名

**主要方法**：

#### `RankPanel()` - 构造函数
- **作用**：初始化排行榜面板
- **功能**：
  1. 设置 null 布局
  2. 初始化返回菜单按钮

#### `setupButton()`
- **作用**：设置返回菜单按钮
- **功能**：
  1. 设置按钮位置和样式
  2. 添加点击事件（返回菜单并重置菜单状态）
  3. 添加到面板

#### `showRank(String model)`
- **作用**：显示指定模式的排行榜
- **参数**：模式名称（"normal"、"hard"、"crazy"）
- **功能**：
  1. 设置当前模式
  2. 复制对应模式的排行榜数据到显示数组

#### `paintComponent(Graphics g)`
- **作用**：绘制排行榜面板
- **功能**：
  1. 绘制背景
  2. 绘制标题
  3. 绘制排行榜数据

#### `drawRank(Graphics g, int[] rank)`
- **作用**：绘制排行榜数据
- **功能**：
  1. 绘制排名、模式名称、分数
  2. 每行显示一条记录

#### `DrawTitle(Graphics g)`
- **作用**：绘制排行榜标题
- **功能**：
  1. 绘制主标题
  2. 绘制列标题（排名、模式、分数）

#### `updateRank(int score, String model)`
- **作用**：更新排行榜数据
- **参数**：
  - `score`：游戏得分
  - `model`：游戏模式
- **功能**：
  1. 根据模式选择对应的排行榜数组
  2. 调用 `insertScore()` 插入分数
  3. 调用 `saveRankings()` 保存到文件

#### `insertScore(int[] rank, int score)`
- **作用**：插入分数到排行榜
- **参数**：
  - `rank`：排行榜数组
  - `score`：要插入的分数
- **功能**：
  1. 遍历排行榜数组
  2. 找到第一个比新分数小的位置
  3. 将后面的分数后移
  4. 插入新分数

#### `loadRankings()` - 数据加载方法
- **作用**：从文件加载排行榜数据
- **功能**：
  1. 创建 Properties 对象
  2. 打开文件输入流
  3. 调用 `load()` 加载数据
  4. 使用 `containsKey()` 检查键是否存在
  5. 使用 `getProperty()` 读取值（带默认值）
  6. 使用 `Integer.parseInt()` 转换为整数
  7. 关闭文件流
- **异常处理**：
  - `FileNotFoundException`：文件不存在，使用默认数据
  - `IOException`：其他 IO 错误

#### `saveRankings()` - 数据保存方法
- **作用**：保存排行榜数据到文件
- **功能**：
  1. 创建 Properties 对象
  2. 使用 `String.valueOf()` 将整数转换为字符串
  3. 使用 `setProperty()` 存储键值对
  4. 创建文件输出流
  5. 调用 `store()` 保存到文件
  6. 关闭文件流
- **异常处理**：捕获 IOException 并输出错误信息

---

### 5. Snake.java - 蛇类

**职责**：蛇的移动、方向控制、碰撞检测

**主要成员变量**：
- `skx[]`：蛇的 x 坐标数组
- `sky[]`：蛇的 y 坐标数组
- `length`：蛇的长度
- `direction`：当前移动方向

**主要方法**：

#### `init()`
- **作用**：初始化蛇
- **功能**：
  1. 设置初始长度（10）
  2. 设置初始位置（横向排列）
  3. 设置初始方向（向右）

#### `move()`
- **作用**：移动蛇
- **功能**：
  1. 将蛇身节点向前移动
  2. 根据方向移动蛇头
  3. 检查边界碰撞
  4. 调用 `touchJudge()` 检查自身碰撞
  5. 如果碰撞，设置死亡状态

#### `touchJudge()`
- **作用**：检测蛇是否撞到自己
- **功能**：
  1. 遍历蛇身节点
  2. 检查蛇头是否与蛇身重合
  3. 如果重合，设置死亡状态

#### `keyPressed(KeyEvent e)`
- **作用**：处理键盘输入
- **功能**：
  1. 根据按键改变方向
  2. 防止反向移动（如向右时不能向左）

#### `getLength()` / `setLength(int newLength)`
- **作用**：获取/设置蛇的长度
- **限制**：长度范围 1-199

#### `getDirection()` / `setDirection(String newDirection)`
- **作用**：获取/设置移动方向
- **限制**：方向只能是 "R"、"L"、"U"、"D"

---

### 6. Food.java - 食物类

**职责**：食物位置生成、碰撞检测、速度控制

**主要成员变量**：
- `fx`：食物 x 坐标
- `fy`：食物 y 坐标
- `speedChangeRate`：速度变化率（Crazy 模式使用）

**主要方法**：

#### `init()`
- **作用**：初始化食物
- **功能**：设置食物到初始位置

#### `create()`
- **作用**：随机生成食物
- **功能**：
  1. 随机生成坐标
  2. 调用 `Judge()` 检查是否与蛇身重合
  3. 如果重合，重新生成

#### `eat()`
- **作用**：处理吃食物事件
- **功能**：
  1. 检查蛇头是否与食物重合
  2. 增加蛇的长度
  3. 增加游戏分数（+10）
  4. 减少游戏速度（加快游戏）
  5. 设置最小速度限制（30ms）
  6. 更新定时器延迟
  7. 重新生成食物

#### `Judge(int x, int y)`
- **作用**：检查坐标是否与蛇身重合
- **参数**：要检查的坐标
- **返回**：true（不重合）/ false（重合）

#### `setSpeedChangeRate(int speedChangeRate)` / `getSpeedChangeRate()`
- **作用**：设置/获取速度变化率
- **用途**：Crazy 模式中每次吃食物速度变化量

#### `getFx()` / `getFy()`
- **作用**：获取食物坐标
- **返回**：x 或 y 坐标

---

## 数据流程

### 游戏启动流程
```
1. Main.main() 启动程序
2. 创建三个面板（GamePanel、MenuPanel、RankPanel）
3. 显示菜单页面
4. 等待用户选择模式
```

### 游戏开始流程
```
1. 用户点击模式按钮（Normal/Hard/Crazy）
2. MenuPanel 设置游戏模式
3. Main.turnPage("game") 切换到游戏页面
4. GamePanel 初始化游戏状态
5. GamePanel 请求焦点
6. 用户按空格键开始游戏
7. Timer 开始定时触发游戏循环
```

### 游戏循环流程
```
1. Timer 触发（每 speed 毫秒一次）
2. Snake.move() 移动蛇
3. Food.eat() 检查吃食物
4. GamePanel.paintComponent() 重绘界面
```

### 游戏结束流程
```
1. 蛇撞墙或撞到自己
2. Snake.touchJudge() 或边界检测设置死亡状态
3. GamePanel 显示游戏结束消息
4. GamePanel 更新排行榜数据
5. RankPanel.saveRankings() 保存到文件
6. 显示返回菜单和查看排名按钮
```

### 排行榜查看流程
```
1. 用户点击 Ranking List 按钮
2. MenuPanel 进入排行榜选择模式
3. Ranking List 按钮隐藏
4. 用户点击模式按钮（Normal/Hard/Crazy）
5. MenuPanel 传递模式名称
6. RankPanel.showRank() 显示对应排行榜
7. RankPanel.loadRankings() 从文件加载数据
```

### 数据持久化流程
```
保存流程：
1. 游戏结束
2. RankPanel.updateRank() 更新排行榜
3. RankPanel.insertScore() 插入分数
4. RankPanel.saveRankings() 保存到文件
   - 创建 Properties 对象
   - String.valueOf() 转换数据
   - setProperty() 存储键值对
   - store() 写入文件

加载流程：
1. RankPanel 静态初始化
2. RankPanel.loadRankings() 加载数据
   - 创建 Properties 对象
   - load() 从文件读取
   - containsKey() 检查键
   - getProperty() 获取值
   - Integer.parseInt() 转换类型
```

## 关键技术点

### 1. Properties 类使用
- **load()**：从文件加载键值对
- **store()**：保存键值对到文件
- **setProperty()**：设置键值对
- **getProperty()**：获取键对应的值（支持默认值）
- **containsKey()**：检查键是否存在

### 2. 类型转换
- **Integer.parseInt()**：字符串转整数
- **String.valueOf()**：其他类型转字符串

### 3. Swing 组件
- **JPanel**：面板容器
- **JButton**：按钮组件
- **JFrame**：主窗口
- **CardLayout**：卡片布局（页面切换）
- **Timer**：定时器（游戏循环）

### 4. 图形绘制
- **paintComponent()**：自定义绘制方法
- **Graphics**：绘图上下文
- **Color**：颜色设置
- **Font**：字体设置

### 5. 事件处理
- **KeyListener**：键盘事件
- **ActionListener**：按钮点击事件
- **MouseListener**：鼠标事件

## 游戏模式说明

### Normal 模式
- **特点**：标准游戏速度
- **初始速度**：250ms
- **速度变化率**：0（不变化）
- **排行榜**：normalRank 数组

### Hard 模式
- **特点**：快速游戏
- **初始速度**：100ms
- **速度变化率**：0（不变化）
- **排行榜**：hardRank 数组

### Crazy 模式
- **特点**：速度递增
- **初始速度**：250ms
- **速度变化率**：10（每次吃食物加快10ms）
- **排行榜**：crazyRank 数组

## 文件说明

### rankings.dat
- **格式**：Properties 格式（key=value）
- **内容**：
  ```
  #Snake Game Rankings
  #时间戳
  normal0=100
  normal1=90
  ...
  hard0=100
  hard1=90
  ...
  crazy0=100
  crazy1=90
  ...
  ```
- **位置**：项目根目录
- **作用**：持久化保存排行榜数据

## 扩展建议

### 1. 添加音效
- 吃食物音效
- 游戏结束音效
- 按钮点击音效

### 2. 添加动画
- 蛇移动动画
- 食物闪烁效果
- 按钮悬停效果

### 3. 添加更多模式
- 时间挑战模式
- 障碍物模式
- 多人模式

### 4. 优化性能
- 使用双缓冲减少闪烁
- 优化绘制逻辑
- 减少不必要的重绘

### 5. 添加设置功能
- 自定义速度
- 自定义颜色
- 自定义控制键

---

**文档版本**：1.0
**最后更新**：2025-03-04
**作者**：贪吃蛇游戏开发团队
